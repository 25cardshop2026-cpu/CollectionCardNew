node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store
Thumbs.db

.vercel

# ข้อมูลที่แอดมินแก้ระหว่างพัฒนา — เป็น runtime state ไม่ใช่ซอร์ส
data/overrides.json

.env*

# HTML ดิบที่สคริปต์ import ดึงมาแคชไว้ ไม่ใช่ซอร์ส
.cache/

# บัญชีผู้ใช้และพอร์ตการ์ดตอนพัฒนาในเครื่อง — runtime state เหมือนกัน
data/users.json
data/portfolios/
